# FraudRuleEngine — server

See the [root README](../README.md) for the full guide: architecture, fraud rules, Docker quickstart, configuration, API reference, database migrations, and testing.

Quick links if you're already in this directory:

```bash
docker compose up --build      # run the API + Postgres
dotnet test                    # run the test suite
```
